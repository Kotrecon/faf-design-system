/**
 * @module Font Weights
 * @description Typographic font weights used across the design system.
 *              Вес шрифтов, используемый в дизайн-системе.
 */

export const fontWeights = {
  /** Lightest, for monospace code / Самый тонкий для моноширинного кода */
  light: 300,
  /** For main body text / Для основного текста */
  normal: 400,
  /** For emphasis (medium bold) / Для выделения (полужирное) */
  medium: 500,
  /** For subheadings / Для подзаголовков */
  semibold: 600,
  /** For headings / Для заголовков */
  bold: 700,
} as const;
